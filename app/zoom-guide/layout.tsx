// /zoom-guide (줌 연동 가이드) 공개 셸 — 로그인 상태에선 인증 영역과 동일한 셸을,
// 비로그인 방문자에겐 공개 헤더를 유지한다(AppShellLayout 이 children 만 반환).
// /resume-guide 와 동일 패턴.
export { default } from "@/app/components/AppShellLayout";
